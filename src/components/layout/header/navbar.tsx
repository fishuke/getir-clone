import * as React from 'react';
import Box from '@material-ui/core/Box';

export default function Navbar() {
    return (
        <Box sx={{height: '45px', backgroundColor: "primary.dark"}}>

        </Box>
    );
}